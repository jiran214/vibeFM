请根据节目基础信息和压缩歌单，设计选歌的整体思路，并挑选 {{count}} 首歌曲。

节目描述：
{{info_json}}

压缩歌单：
{{playlist_json}}

每首歌曲格式为 `[id, 标题, 歌手名数组, 专辑名]`。

只输出以下 JSON，不能增加字段：

{
  "think": "如何设计这期电台节目歌单、播放顺序",
  "track_ids": [123, 456]
}

要求：
- `think` 必须是非空字符串。
- `track_ids` 必须恰好包含 {{count}} 个不同歌曲 ID。
- ID 必须来自压缩歌单，数组顺序就是节目播放顺序。
